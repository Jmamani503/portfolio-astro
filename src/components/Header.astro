<nav
  class="fixed top-0 w-full h-auto bg-[#1A202C] flex flex-col md:flex-row md:justify-between  py-5 px-12 z-30 border-b-2 border-[#00BD95]"
>
  <div class="flex justify-between w-full ">
    <h3 class="text-xl font-bold text-[#00BD95]">{'< Jaime />'}</h3>
    <button 
      id="menu-toggle" 
      class="block md:hidden text-white focus:outline-none"
    >
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>
  </div>

  <div  id="menu" class="flex items-center flex-col md:flex md:flex-row hidden gap-6 ">
    <a
      class="text-xl font-bold transition ease hover:scale-110 hover:text-[#00BD95] duration-300"
      href="#home"
    >
      Home
    </a>
    <a
      class="text-xl font-bold transition ease hover:scale-110 hover:text-[#00BD95] duration-300"
      href="#experience"
    >
      Experience
    </a>
    <a
      class="text-xl font-bold transform ease hover:scale-110 hover:text-[#00BD95] duration-300"
      href="#projects">Projects</a
    >
    <a
      class="text-xl font-bold transform ease hover:scale-110 hover:text-[#00BD95] transition duration-300"
      href="#about">About</a
    >
  </div>
</nav>

<script>
    const toggleMenu = document.getElementById('menu-toggle') as HTMLButtonElement;
    toggleMenu.addEventListener('click', () => {
      const menu = document.getElementById('menu') as HTMLButtonElement;
      menu.classList.toggle('hidden');
    })
</script>

